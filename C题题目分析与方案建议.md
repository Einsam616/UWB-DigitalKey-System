# C题题目分析与方案建议

分析依据：`C题_基于无线通信的数字钥匙实验系统.pdf`（4页）

## 1. 结论先行

本题可以做，但它本质上不是普通的“无线收发 + 门锁控制”题，而是一道小基线无线定位题。真正决定成败的是：所有智能门锁硬件必须放在直径 60 cm、高 30 cm 的圆柱体内，却要在圆柱体边界外 0~3 m、正前方 +/-45 度范围内，同时测出距离和方位，并具有抗多径能力。

推荐的主路线是 UWB，不建议把 BLE/Wi-Fi/LoRa/nRF24 的 RSSI 当作主定位量：

- 数字钥匙：1 个 UWB 节点，按键启动后周期广播 4 位 ID，并参与双边双向测距（DS-TWR）。
- 智能门锁：3 个 UWB 测距节点尽量沿 60 cm 圆柱边界拉开基线，外加 1 个主控。
- 每个门锁节点独立测得到钥匙的距离，主控用已标定的节点坐标解算钥匙二维坐标。
- 以圆柱中心为坐标原点，最终显示的径向距离必须按 `d = sqrt(x^2+y^2) - R` 计算，其中 `R` 是现场采用的圆柱边界半径，不能直接把某个天线到钥匙的距离当成题目要求的径向距离。
- 方位角按 `alpha = atan2(x, y)` 计算，并只对正前方 +/-45 度内的钥匙执行区域判决。

若已经有成熟、可直接输出距离和角度的 UWB PDoA/AoA 开发套件，可优先验证该路线；但不建议在三天竞赛中从零做双天线射频板和相位标定。若只有普通单天线 UWB 模块，三节点测距 + 几何定位更可控。

选题门槛：至少应马上确认有 1 个钥匙节点加 2 个门锁 UWB 节点；推荐总计 4 个兼容 UWB 节点（1 个钥匙、3 个门锁）。如果没有 UWB 或其他已验证的无线测距/测向硬件，单靠 RSSI 临时拼方案，完成第 3~5 项的风险很高。

## 2. 题目要求拆解

| 模块 | 硬性要求 | 设计目标/解释 |
|---|---|---|
| 数字钥匙 | 一键启动后持续发送 ID；电池供电；尺寸不大于 12 cm x 8 cm x 4 cm | 按键后保持工作，不应要求持续按住；ID 应放入带序号和 CRC 的无线帧；在外壳上标出定位点，最好对应 UWB 天线相位中心 |
| 身份验证 | ID 为 4 位二进制数；门锁必须有 4 位拨码开关，可手动修改待验证 ID | 拨码开关是题面指定器件，不能只用屏幕菜单代替；无效 ID 也要显示识别结果，但不得迎宾或开锁 |
| 通信 | 通信距离不小于 3 m；方式不限，但禁用超声和光通信 | 工程上按至少 3.5 m 留裕量；ID 广播和定位最好共用 UWB，减少系统割裂 |
| 定位范围 | 门锁正前方 +/-45 度；感应区最远距离不得小于 3 m | +/-45 度边界要覆盖；区外目标应显示或忽略均可，但不能误触发门锁动作 |
| 定位精度 | 径向距离绝对误差不大于 0.3 m；方位角绝对误差不大于 10 度 | 这是全题最高风险项，必须在完整扇区和多个距离点上验证，不能只测正前方 |
| 开锁区 | 圆柱边界外 0~1 m；进入开锁，离开闭锁；边界判定误差不大于 0.2 m | 需要有方向性的进入/离开事件和滞回，不能只显示当前距离 |
| 迎宾区 | 圆柱边界外 1~2 m；进入声光提示，离开关闭；边界判定误差不大于 0.2 m | 声光提示与门锁红/绿状态灯应区分，避免显示含义混乱 |
| 感应区 | 2 m 以外，最远至少到 3 m | 一进入即接收 ID、验证并持续定位 |
| 门锁外形 | 全部硬件位于直径 60 cm、高 30 cm 圆柱体内 | 不允许把定位基站放到场地四周；建议做明确的 60 cm 圆形底板/边界，既扩展测向基线又固定测距零点 |
| 门锁显示 | 安装在门锁背面；实时显示 ID、位置、判决结果和开/闭状态 | 至少显示接收 ID、设定 ID、合法/非法、径向距离、带正负号角度、当前区域、最近事件、锁状态 |
| 门锁动作 | 不同颜色 LED 模拟开锁/闭锁，并在屏幕显示开/闭状态 | 无须机械锁舌，不要把时间耗在机械结构上 |
| 环境 | 正前方扇区内测试时无其他物体；作品要抗多径，不能要求特殊场地 | 不可依赖外置标志物、外部基站或特制反射环境；应记录 UWB 首径质量并做异常值抑制 |

## 3. 题面中的一个正式疑点

PDF 第 2 页的“要求”编号确实从第 1 项直接跳到第 3 项，并非文本提取遗漏；但第 4 页评分表又写有“完成第 1 项 12 分、完成第 2 项 8 分”。正文第 1 项本身标为 20 分。

最合理的暂时理解是：评分时可能把“ID 检测/接收/显示”和“通信距离不小于 3 m”拆成 12 分、8 分，但题面没有给出正式的第 2 项文字。不能把这一推测当成官方结论。比赛现场应尽快向组委会/巡视员确认；在得到答复前，把正文第 1 项的全部内容都实现，即可覆盖这 20 分。

## 4. 为什么优先选 UWB

### 4.1 方案比较

| 方案 | 距离 | 角度 | 多径表现 | 三天实现风险 | 结论 |
|---|---|---|---|---|---|
| BLE/Wi-Fi/LoRa RSSI | 易受发射功率、人体、方向和场地影响 | 多天线 RSSI 差值很不稳定 | 差 | 表面低、实际调试失控 | 不作为主方案 |
| Wi-Fi FTM | 可做测距，但终端和驱动支持受限 | 仍需多节点或阵列 | 中 | 高 | 不优先 |
| BLE 5.1 AoA + 另一种测距 | 角度可行 | 需要天线阵列、RF 开关和 IQ 标定 | 中 | 很高 | 仅适合已有成熟平台 |
| 毫米波雷达 + BLE ID | 雷达距离、角度能力强 | 能测反射目标 | 把雷达目标与 ID 钥匙绑定存在解释风险，静止小钥匙也可能难检出 | 高 | 可作备选验证，不作为首选 |
| UWB PDoA/AoA | 同一无线链路可测距和测角 | 硬件支持时最直接 | 较好 | 成熟套件低，自制 RF 板极高 | 有现成套件时优先 |
| 多节点 UWB DS-TWR | 厘米到分米级，容易做绝对距离 | 利用 50~58 cm 基线的距离差解算 | 较好，可读取质量指标 | 中 | 普通 UWB 模块的推荐路线 |

### 4.2 角度指标对测距精度的反推

设门锁左右两个 UWB 天线的基线为 `B`，钥匙距离远大于基线时，有近似关系：

```text
Delta_r = r_left - r_right ~= B * sin(alpha)
```

若基线做到 `B = 0.54 m`，在最不利的 `alpha = 45 deg` 附近，要把角度误差控制在 `10 deg` 内，允许的左右距离差误差约为：

```text
B * cos(45 deg) * 10 deg(in radians) ~= 0.067 m
```

也就是左右测距的差分误差必须控制在约 6.7 cm 内。为了留出多径、运动延迟和标定误差的裕量，建议把滤波后的差分随机误差目标定为 3 cm 以内，并逐节点标定固定天线延时。

简化蒙特卡洛计算也说明了这一点：在约 54 cm 基线、3 个门锁节点的几何下，若每次独立测距噪声标准差约 3 cm，最不利角度点的 95% 角度误差仍可能略超过 10 度；只有通过高速多次测量、稳健平均、节点延时标定和异常值剔除，把有效差分误差进一步压低，才有稳定余量。因此，“能显示一个角度”不代表通过精度测试。

## 5. 推荐系统结构

### 5.1 数字钥匙

1. 电池、稳压、电源开关/启动键、低功耗 MCU、单天线 UWB 模块。
2. 按键启动后周期发送包含 `preamble + type + sequence + 4-bit ID + CRC` 的广播帧。
3. 响应门锁各节点的 DS-TWR 测距帧；建议 ID 也出现在测距帧中，避免定位结果与身份帧错配。
4. 在外壳明确标注定位点，定位点尽量落在 UWB 天线相位中心的垂直投影处。
5. 天线周围禁布电池、大面积铜、屏幕和金属件；测试时固定钥匙的朝向和极化方向，至少先保证题面场景稳定。

### 5.2 智能门锁

1. 使用直径 60 cm 圆形底板或明确的圆柱边界；将左右 UWB 天线尽量布到边界附近，最大化有效基线。
2. 推荐 3 个已知坐标的 UWB 测距节点：左右节点负责主要测向，第三节点提供冗余、距离约束和异常检测。
3. 主控依次调度节点与钥匙进行 DS-TWR，避免无线帧碰撞；可由一个主控控制多个收发器，也可由各节点 MCU 测距后通过有线串口/总线汇总。
4. 解算二维位置 `(x,y)`，选取正前方的几何解；再换算题目定义的边界距离和方位角。
5. 屏幕安装在背面；4 位拨码开关、蜂鸣器/迎宾灯、红绿锁状态 LED 均由主控管理。
6. 设置丢包超时：合法钥匙在开锁区失联时，应在短超时后回到闭锁状态，避免保持开锁。

### 5.3 高度和零点

UWB 测得的是两天线之间的三维直线距离，而题目考核的是场地平面上的径向距离。门锁天线与钥匙天线若存在高度差 `h`，应使用 `r_horizontal = sqrt(r_uwb^2 - h^2)` 修正，或把门锁与钥匙天线布置在相同高度。

必须统一三种点：

- 题目径向零点：直径 60 cm 圆柱的边界；
- 角度原点：圆柱中心及其正前方轴线；
- 钥匙测试点：钥匙外壳标注的定位点。

这三者不校准，即使 UWB 原始测距很准，显示值也会产生固定系统误差。

## 6. 判决状态机

建议按“身份有效 + 位置有效 + 历史状态”共同判决：

```text
未检测到/超时
  -> 检测到合法 ID，d > 2 m：感应区
  -> d 下降并越过约 1.95 m：进入迎宾区，开启声光
  -> d 下降并越过约 0.95 m：进入开锁区，点亮开锁 LED
  -> d 上升并越过约 1.05 m：离开开锁区，点亮闭锁 LED
  -> d 上升并越过约 2.05 m：离开迎宾区，关闭迎宾声光
```

以上 5 cm 滞回只是初始建议，最终应依据实测噪声和测试移动速度调整，但不能把滞回做得超过 +/-0.2 m 的边界误差要求。所有进入/离开事件都应短时保留在屏幕上，不能一闪而过。

非法 ID：显示接收 ID、设定 ID 和“验证失败”，可继续显示测得位置，但不得触发迎宾和开锁。拨码开关变化后应消抖并立即更新设定 ID。

## 7. 抗多径和稳定性措施

1. 优先使用 DS-TWR，降低两端时钟偏差对测距的影响。
2. 对每个 UWB 节点分别做天线延时和固定偏差标定，参数写入非易失存储器。
3. 使用滑动中值/截尾平均，而不是只用普通均值；对突跳值和不可能的运动速度做剔除。
4. 若芯片可提供首径功率、接收功率、CIR/STS 质量等指标，质量差的测距降权或丢弃。
5. 三节点定位使用加权最小二乘或残差最小的两节点组合；第三节点残差过大时显示定位质量下降，而不是输出剧烈跳动的角度。
6. 天线高度、朝向、极化和周边净空保持一致；显示屏、电池、金属支架与 UWB 天线分区。
7. 滤波窗口必须兼顾延迟。若测试者移动速度为 `v`、处理延迟为 `T`，边界触发额外误差约为 `v*T`。建议最终位置更新达到 10 Hz 以上，端到端延迟控制在约 200 ms 内。
8. 至少在不同房间/走廊重复测试，不能只在一个已调参场地通过。

## 8. 显示与操作建议

背面显示建议固定布局，避免评委寻找信息：

```text
RX ID: 1010    SET ID: 1010    VALID
DIST: 1.36 m   ANGLE: -22 deg  QUALITY: OK
ZONE: WELCOME  EVENT: ENTER WELCOME
LOCK: CLOSED   LINK: 16 Hz
```

锁状态用两种颜色 LED 明确区分；迎宾使用独立蜂鸣器和独立灯。屏幕上的距离应明确是“距圆柱边界”的距离，角度应带正负号并与底板标注方向一致。

## 9. 得分和时间优先级

功能 100 分中，定位、迎宾和开闭锁三项合计 66 分；通信/ID 20 分、拨码验证 10 分、其他 4 分。设计报告另计 20 分。

建议按以下顺序推进：

1. 当天先证明 UWB 链路在 3.5 m 仍可持续收发 ID，并在正前方完成稳定测距。
2. 把左右节点拉到最大基线，先只验证 `0 deg、+/-30 deg、+/-45 deg` 的角度误差；若最外侧角度不能进 10 度，立即调整定位路线，不要先做 UI。
3. 完成 0~3 m 的二维标定和误差表，再实现迎宾/开锁状态机。
4. 加入 4 位拨码、背面显示、声光和红绿锁灯，拿稳容易分。
5. 最后做外壳、抗多径复测、异常状态和“其他”功能。

报告从第一轮测试就开始记录：节点坐标、天线高度、原始距离、滤波后距离、真实位置、误差、不同场地结果都要留表。报告的“理论分析与计算 + 电路与程序设计 + 测试结果”占 15/20 分，定位公式、误差反推和实测误差矩阵正好是报告主线。

## 10. 首轮验证矩阵和停止条件

建议在底板定型前完成以下最小矩阵，每点静态采样不少于 100 组：

- 径向距离：0.2、0.5、0.8、1.0、1.2、1.8、2.0、2.2、3.0、3.3 m；
- 方位角：-45、-30、-15、0、15、30、45 度；
- 至少两个普通室内场地；
- 每点记录有效率、均值误差、最大绝对误差、标准差、95% 误差和更新延迟。

继续采用该定位路线的最低条件：

- 3.3 m 通信稳定；
- 关键测试点的径向误差有明显余量，目标不超过 0.15 m；
- 方位角 95% 误差目标不超过 7 度；
- 1 m、2 m 边界的重复触发位置落在 +/-0.1 m 左右；
- 连续更新延迟不使移动测试的边界误差超过 0.2 m。

若最大基线、标定和稳健滤波后仍无法把 +/-45 度角度压到要求内，应尽早转向已校准的 UWB AoA/PDoA 硬件，而不是继续增加 RSSI、经验补偿或场地专用参数。

## 11. 当前需要立即确认的物料信息

下一步方案能否落到具体电路，取决于以下库存：

- UWB 模块的准确型号、数量、配套开发板和现有例程；
- 可用 MCU/开发板型号与数量；
- 显示屏型号和接口；
- 电池、拨码开关、蜂鸣器、红绿 LED；
- 是否已有 UWB AoA/PDoA 套件或带坐标输出的成品定位模块；
- 队员是否做过 DW1000/DW3000、BLE AoA 或毫米波雷达。

器件库存确认后，才能在“普通 UWB 多节点定位”和“成熟 UWB AoA/PDoA”之间做最终选择，并给出具体接线、任务分工和小时级进度表。
