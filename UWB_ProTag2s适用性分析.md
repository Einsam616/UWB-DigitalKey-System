# UWB ProTag2s 对 C 题的适用性分析

分析依据：

- `C题_基于无线通信的数字钥匙实验系统.pdf`
- `um010-UWB_ProTag2s使用手册_V1.2.pdf`
- 手册版本 V1.2，文档生成时间 2019 年 5 月；其中涉及的 ProTag2s 出厂固件版本为 1.8.7.7，部分 AT 指令要求 1.9.4 以上固件。

## 1. 结论

UWB ProTag2s **可以作为本题的数字钥匙使用**，而且尺寸、电池、一键开关、持续 UWB 工作、标签 ID 和 TWR 测距能力都与题目高度匹配。

但一只 ProTag2s 不能单独完成本题。按照该产品体系，需要配套兼容基站（手册推荐 Mini3sPlus）获得标签到多个基站的距离。该系列没有原生方位角输出，方位角必须由多个基站测距后计算二维坐标，再由坐标换算。

对于“全部完成、冲省一/省二”的目标，建议最终系统使用：

- 实物工作节点：1 个 ProTag2s 标签 + 4 个兼容 UWB 基站；
- 备件：额外准备 1 个标签和 1 个基站；
- 门锁主控：STM32F407，通过 A0 基站的 TTL 串口读取 TOF Report Message；
- 算法：四基站加权最小二乘 + 单基站标定 + 异常值剔除 + 滑动稳健滤波；
- 蓝牙不进入定位与门锁判决链路。

该方案具有实现全部题目功能的基础，但不能仅依据手册中“测距误差小于 10 cm、定位误差小于 15 cm”的宣传参数直接判定通过。手册中的定位精度通常对应基站相距数米的常规 RTLS 场景，而本题把全部基站限制在直径 60 cm 内，方位角的几何条件明显更差。必须完成 60 cm 小基线实测后才能最终确认。

## 2. 与题目要求的对应关系

| 题目要求 | ProTag2s 能力 | 判断 |
|---|---|---|
| 钥匙尺寸不大于 12 cm x 8 cm x 4 cm | 外壳约 51 mm x 36 mm x 15 mm | 满足 |
| 电池供电 | 内置 250 mAh 可充电锂电池和充放电管理 | 满足，需实测连续运行时间 |
| 一键启动后持续发送 ID | 具有开机按键；作为 RTLS Tag 持续参与 TWR | 基本满足 |
| 4 位二进制身份 ID | 协议中有 Tag short ID，范围 0~31 | 可将 0~15 映射为题目 4 位 ID |
| 通信距离不小于 3 m | 手册标称与 Mini3sPlus 配合无遮挡可超过 300 m | 3 m 链路余量充足 |
| 径向距离误差不大于 0.3 m | TWR，典型抖动约 +/-10 cm；一般遮挡约 +/-30 cm | 有基础，但必须重新标定 |
| 方位角误差不大于 10 度 | 没有原生 AoA；可由 3/4 基站距离解算坐标 | 最大风险项 |
| 迎宾/开锁边界误差不大于 0.2 m | 可输出毫米单位测距和最高约 25 Hz 定位周期 | 有实现条件，需控制滤波延迟 |
| 抗多径且不能要求特殊场地 | UWB 相对 RSSI 有优势，但手册仍要求远离金属并正确安装天线 | 需要结构设计和跨场地验证 |
| 门锁显示 ID、位置和状态 | A0 输出距离、Tag ID 和序号，主控可自行显示 | 满足开发条件 |

## 3. 可直接复用的产品能力

### 3.1 数字钥匙硬件

ProTag2s 已集成：

- STM32F103T8U6；
- DW1000 UWB；
- 陶瓷板载天线；
- 250 mAh 锂电池与充放电管理；
- LIS3DH 加速度计；
- Micro-USB；
- 开机按键、工作 LED 和蜂鸣器。

因此不必重新制作第一版钥匙射频硬件。外壳上需要新增明确的定位点标记，定位点应尽量对应陶瓷天线相位中心的垂直投影。

### 3.2 ID 映射

TOF Report Message 的末尾字段为 `aT:A`，其中 `T` 是 Tag short ID、`A` 是 Anchor ID。标签地址可设置为 0~31，题目只需要 0~15。

建议直接规定：

```text
题目4位ID = Tag short ID 的低4位
```

门锁拨码开关读取 0~15，与收到的 Tag ID 比较。这样不需要增加独立蓝牙模块，也不需要另造一套身份协议。若现场要求展示二进制形式，屏幕同时显示 `ID=10 / 1010B`。

需要注意：手册第 22 页说明 `AT+QSET` 需要 1.9.4 以上固件，手册前文的出厂固件却是 1.8.7.7。必须从公司获得匹配硬件的最新固件或源码，不能假定旧设备一定支持该指令。

### 3.3 测距数据接口

A0 基站可输出 ASCII 格式 TOF Report Message，例如：

```text
mc 0f RANGE0 RANGE1 RANGE2 RANGE3 NRANGES RSEQ DEBUG aT:A
```

- `mc`：优化修正后的标签到基站距离；
- `MASK`：哪些 RANGE 字段有效；
- `RANGE0~3`：标签到 A0~A3 的距离，十六进制毫米值；
- `NRANGES/RSEQ`：测距和帧序号；
- `T`：Tag short ID；
- `A`：输出数据的 Anchor ID。

主控不需要运行旧的 PC-RTLS 上位机，直接解析 A0 的 TTL 串口即可。手册的产品对比表说明 Mini3sPlus 有 TTL 串口，但当前目录没有 Mini3sPlus 原理图和引脚定义，因此接线前必须取得对应基站手册/原理图。

## 4. 推荐的门锁基站布置

定义门锁圆柱中心为 `O=(0,0)`，正前方为 `+Y`，圆柱半径按 0.30 m 建模。建议将 4 个 UWB 天线相位中心布置为：

```text
A0 = (-0.27,  0.00, h)
A1 = (+0.27,  0.00, h)
A2 = ( 0.00, +0.27, h)
A3 = ( 0.00, -0.27, h)
```

其中 `h` 为统一天线高度，建议在不超过 30 cm 总高度的前提下，尽量把天线相位中心放到 15~25 cm，并让钥匙天线测试点处于同一高度。具体坐标必须以实物天线相位中心复测，不应直接使用 PCB 边缘坐标。

四基站十字布局的理由：

- 左右 54 cm 基线提供主要的角度信息；
- 前后 54 cm 基线补充径向约束，并改善 +/-45 度边缘的几何条件；
- 第四个距离可用于残差检查和单个基站异常剔除；
- 所有节点仍位于直径 60 cm 圆柱内。

底板和支架必须使用非金属材料，天线竖直、同向、同极化，屏幕、电池、主控和大面积金属走线应远离天线净空区。

## 5. 坐标和区域计算

主控使用 4 个基站坐标及 4 个测距值进行二维加权最小二乘，得到钥匙天线坐标 `(x,y)`。题目所需结果为：

```text
center_range = sqrt(x*x + y*y)
boundary_range = center_range - 0.30
angle = atan2(x, y)
```

必须显示 `boundary_range`，不能显示某个基站到标签的原始距离，也不能直接显示到圆心的距离。

如果标签天线与基站天线存在已知高度差 `dz`，每个 UWB 三维距离应先转换为平面距离：

```text
horizontal_range = sqrt(uwb_range*uwb_range - dz*dz)
```

## 6. 小基线精度风险估算

使用手册给出的单次典型 `+/-10 cm` 抖动，对 60 cm 内四基站十字布局做简化蒙特卡洛估算：

- 单帧直接定位时，方位角 95% 误差可能约 15 度，不能稳定通过；
- 5 帧中值后，方位角 95% 误差仍可能约 10~11 度，余量不足；
- 9 帧中值后，在纯独立随机误差假设下，方位角 95% 误差可降到约 8 度。

上述计算只是噪声敏感度估算，不包含固定天线延时、地面反射和多径偏差。真实系统中的固定偏差不会通过增加滤波窗口消失，必须逐基站标定。

手册给出可通过修改时隙把定位周期提高到约 25 Hz。9 帧滑动窗口约覆盖 360 ms，重叠更新仍可达到实时显示，但运动时会带来延迟。因此实现时不应简单输出9帧中值，而应采用：

1. 每基站短窗口中值，剔除跳点；
2. 四基站加权最小二乘和残差检查；
3. 对最终 `(x,y)` 使用 alpha-beta 或常速度滤波；
4. 依据速度预测补偿约半个窗口的延迟；
5. 区域状态机使用约 5 cm 滞回，并以实测触发误差重新调整。

## 7. 需要从公司取得的资料

当前项目目录只有使用手册，没有配套代码。向公司索取时，应明确需要以下内容：

1. ProTag2s 和 Mini3sPlus 的完整 Keil 工程源码，而不是只有 HEX；
2. 与实物硬件版本匹配的原理图、引脚定义和 BOM；
3. DW1000 驱动、TWR 协议和时隙调度源码；
4. 标签和基站地址配置源码/AT 固件，确认支持 Tag ID 0~15；
5. `mr/mc/ma` 数据帧生成代码和协议文档；
6. `trilateration.cpp` 或 Tag 手持器坐标解算源码；
7. 天线延时标定值、标定方法和每块板的出厂参数；
8. 可读取的首径功率、接收功率、CIR 或其他链路质量指标及接口；
9. 提升到 20~25 Hz 的单标签、四基站配置；
10. 最新版硬件和固件手册，以及源码的使用授权范围。

其中最重要的不是旧 PC 上位机，而是匹配实物的 Anchor/Tag 固件源码、A0 串口输出和天线标定信息。

## 8. 软件改造建议

### UWB 节点固件

- 只保留 1 个标签和 4 个基站，删除无关多标签时隙；
- 关闭不需要的 Anchor-to-Anchor 周期测距，或只在启动标定阶段启用；
- 将稳定输出速率提高到 20 Hz 以上；
- 同时保留原始距离、校准后距离、序号和质量指标；
- 为 A0 增加校验明确的二进制或 ASCII 输出帧；
- 看门狗和失联恢复不能依赖人工重启。

### STM32F407 门锁主控

- `UwbProtocol`：串口接收、帧解析、ID和序号检查；
- `RangeCalibration`：逐基站偏差、比例、温度/批次参数；
- `PositionEstimator`：四基站加权二维解算；
- `PositionFilter`：异常值抑制和低延迟运动滤波；
- `ZoneStateMachine`：感应、迎宾、开锁、离开、超时闭锁；
- `Identity`：4位拨码消抖与Tag ID比较；
- `Display`：ID、距离、角度、区域、事件、锁状态和质量；
- `Diagnostics`：串口日志、丢包率、各基站残差和测试模式。

坐标解算和区域状态机应另建 PC 单元测试，用录制的真实 TOF 数据回放，避免每次算法调整都依赖现场移动硬件。

## 9. 必须完成的首轮实测

### 阶段 A：单链路

- 1 Anchor + 1 Tag；
- 真实距离 0.3、0.5、1.0、2.0、3.0、3.5 m；
- 每点采集至少 1000 帧；
- 统计均值偏差、标准差、95%误差、最大误差、丢包率和更新率；
- 改变标签朝向，检查陶瓷天线方向性。

### 阶段 B：60 cm 小基线定位

- 4 个基站按实际底板坐标固定；
- 距离点：0.2、0.5、0.8、1.0、1.2、1.8、2.0、2.2、3.0、3.3 m；
- 角度点：-45、-30、-15、0、15、30、45 度；
- 每点采集至少 500 个完整四基站样本；
- 在两个不同室内场地重复关键测试点。

### 继续使用这套硬件的门槛

- 3.3 m 持续通信无明显丢失；
- 径向距离 95% 绝对误差不超过 0.15 m；
- 方位角 95% 绝对误差不超过 7 度；
- 1 m、2 m 边界重复触发误差不超过 0.10 m；
- 连续位置输出不低于 10 Hz，端到端延迟不超过约 200 ms；
- 换场地后无需重新拟合一套场地专用公式即可通过。

若四基站、逐节点标定和稳健滤波后，`+/-45度` 方位仍无法达到上述门槛，说明 DW1000 多基站小基线方案的角度余量不足。此时应转向公司更新的 UWB PDoA/AoA 产品，而不是继续增加 RSSI 或场地专用经验补偿。

## 10. 最终判断

| 用法 | 判断 |
|---|---|
| ProTag2s 单独作为整套方案 | 不可行，没有基站和角度信息 |
| ProTag2s 作为数字钥匙 | 很合适 |
| 3 Mini3sPlus + 1 ProTag2s | 能定位，但本题小基线角度余量偏小 |
| 4 Mini3sPlus + 1 ProTag2s | 推荐验证，具备完成全部要求的可能 |
| 直接依赖旧 PC-RTLS 上位机 | 不推荐，比赛实物应由门锁主控独立运行 |
| 有完整源码后进行二次开发 | 可行，且是这套旧平台的主要价值 |
| 用手册宣传精度直接宣称通过 | 不可接受，必须做60 cm布置实测 |

因此，这套产品可以作为当前的第一套原型平台，尤其适合快速证明 ID、测距、显示和区域状态机。是否作为最终冲奖硬件，取决于四基站小基线实测结果，以及公司是否能提供更新的 AoA/PDoA 产品作为备选。
